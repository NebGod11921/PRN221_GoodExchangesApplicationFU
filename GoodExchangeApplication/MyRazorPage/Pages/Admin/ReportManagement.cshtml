@page
@model MyRazorPage.Pages.Admin.ReportManagementModel
@{
}
<div class="container mt-5">
    <h2 class="text-center">Report Management</h2>
    <div class="table-responsive">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Report ID</th>
                    <th>Post ID</th>
                    <th>Reason</th>
                    <th>Date Reported</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
               @*  @if (reportData == null)
                {
                    <tr>
                        <td colspan="5" class="text-center">Loading...</td>
                    </tr>
                }
                else
                {
                    @foreach (var report in reportData)
                    {
                        <tr>
                            <td>@report.ReportId</td>
                            <td>@report.PostId</td>
                            <td>@report.Reason</td>
                            <td>@report.DateReported.ToString("yyyy-MM-dd")</td>
                            <td>
                                <button class="btn btn-danger btn-sm" @onclick="() => HandleDeletePost(report.PostId, report.ReportId)">Delete</button>
                            </td>
                        </tr>
                    }
                } *@
            </tbody>
        </table>
    </div>
</div>